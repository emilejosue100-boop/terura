# Vercel production — REQUIRED: your Render backend URL (no trailing slash)
# Example: https://terura-api.onrender.com
BACKEND_URL=

# Optional: call Render directly instead of Vercel /api proxy (requires CORS on backend)
# VITE_API_URL=

# Local dev uses Vite proxy to http://localhost:5000 — leave both empty locally.
