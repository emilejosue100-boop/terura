# Leave empty for local dev — Vite proxies /api to http://localhost:5000
VITE_API_URL=

# Production (Vercel): set to your Render backend URL, no trailing slash
# VITE_API_URL=https://terura-api.onrender.com
